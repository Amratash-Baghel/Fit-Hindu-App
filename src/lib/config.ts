/**
 * App-level constants that are decisions, not code.
 */

/**
 * Public privacy policy. The DPDP consent screen links to it (onboarding spec
 * :35-37) and Google Play will not let the app publish without a working URL
 * (docs/play-store-setup.md:156).
 *
 * EMPTY UNTIL THE PAGE IS LIVE on herbaldeck.com. While empty the consent
 * screen renders without the link — the itemised notice still shows, so the
 * screen is honest, just not yet complete. Fill this in and the link appears.
 */
export const PRIVACY_POLICY_URL = "";
